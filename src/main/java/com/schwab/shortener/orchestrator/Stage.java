package com.schwab.shortener.orchestrator;

/** The SDLC stages this orchestrator coordinates. */
public enum Stage {
    REQUIREMENTS,
    DESIGN,
    APPROVAL,
    IMPLEMENT,
    DOCS,
    TEST,
    RELEASE_APPROVAL,
    RELEASE
}
