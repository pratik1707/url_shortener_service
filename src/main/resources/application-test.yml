spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop

shortener:
  strategy: counter
  base-url: http://localhost

orchestrator:
  max-attempts: 3
  max-revisions: 2
  llm:
    enabled: false
